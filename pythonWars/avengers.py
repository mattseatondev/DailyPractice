
def avenge(people):
    twentyFives = fifties = hundreds = 0
    for p in people:
        expected_change = p - 25
        if expected_change

    for p in people:
        change.append(p)
        expected_change = p - 25
        if not expected_change | expected_change in change:
            continue
        else:
            for c in change:
                for d in change:
                    if c + d == expected_change:
                        print('here it is ', c + d)
                        continue
                    else: break
    else: return 'YES'
    return 'NO'

    # else: return 'YES'
    # return 'NO'

persons1 = [25, 25, 50]
persons2 = [25, 100]
if __name__ == '__main__': print(avenge(persons1))
if __name__ == '__main__': print(avenge(persons2))
